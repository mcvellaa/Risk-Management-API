<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :path_to_fb_info %><br>
    <%= f.text_field :path_to_fb_info %>
  </div>
  <div class="field">
    <%= f.label :last_logged_on %><br>
    <%= f.datetime_select :last_logged_on %>
  </div>
  <div class="field">
    <%= f.label :date_joined %><br>
    <%= f.datetime_select :date_joined %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
